---
description: 处理 dist 构建产物时提醒确认输出路径与 dist 层级
globs: dist/**
alwaysApply: false
---

# dist 构建产物提醒

- 处理 `dist/**` 下的文件、核对静态资源，或执行构建相关操作时，先提醒用户当前构建输出路径。
- 本项目默认构建输出目录是仓库根目录下的 `dist`。
- 若用户本地部署环境还有额外一层 `dist`，必须明确区分“仓库内构建产物目录”和“本地部署目录”两层路径，避免误判静态资源是否已更新。

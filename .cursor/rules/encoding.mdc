---
description: 项目编码与 Git 提交信息使用 UTF-8，避免乱码
alwaysApply: true
---

# 编码要求

## Git 提交信息必须使用 UTF-8

- 在 Windows 等非 UTF-8 默认环境下，提交信息若按系统编码（如 GBK）写入，在 GitHub 上会显示为乱码。
- 本仓库已配置 Git 使用 UTF-8：
  - `i18n.commitEncoding = utf-8`
  - `i18n.logOutputEncoding = utf-8`
- 执行 Git 相关操作（提交、amend、rebase 等）时：
  - 提交信息（commit message）一律使用 **UTF-8** 书写。
  - 若通过脚本或临时文件传入提交信息，确保该文件以 **UTF-8** 保存（无 BOM 或 UTF-8 BOM 均可）。

## 推送/提交时（Agent 与终端）

- **在 Windows PowerShell 下执行 `git commit` 时**：避免直接用 `-m "中文"`，因控制台可能以 GBK 传参导致 GitHub 上乱码。应二者择一：
  1. **推荐**：先把提交信息写入临时 UTF-8 文件，再用 `git commit -F <文件>` 提交。
  2. 或在执行前设置控制台为 UTF-8：`chcp 65001`，再执行 `git commit -m "..."`（仍可能受终端实现影响）。
- **推送前自检**：若本次会话内刚执行过 `git commit`，且提交信息含中文，在 **push 之前** 用 `git log -1 --format=%B` 检查最后一条提交信息是否乱码；若乱码，用 `git commit --amend -F <UTF-8 文件>` 修正后再推送。

## 新建仓库或克隆后建议

若在其他机器或新克隆的仓库中工作，建议设置编码后再提交：

```bash
git config i18n.commitEncoding utf-8
git config i18n.logOutputEncoding utf-8
```

或全局设置：

```bash
git config --global i18n.commitEncoding utf-8
git config --global i18n.logOutputEncoding utf-8
```

## 源文件与资源

- 源代码、配置、文档等文本文件优先使用 **UTF-8** 编码保存，以便跨平台与 GitHub 上正确显示。
